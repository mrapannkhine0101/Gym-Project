{% if user.profile_picture %}
    <img src="{{ user.profile_picture.url }}" class="card-img-top" alt="Profile Picture">
    {% else %}
    <img src="{% static 'image/mra.png' %}" class="card-img-top w-50" alt="Default Profile Picture">
    {% endif %}